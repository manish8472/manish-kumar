// Defines the properties (props) that the Logo component can accept.
export interface LogoProps {
  className?: string; // Optional custom CSS class for additional styling.
  small?: boolean; // Optional flag to render a smaller version of the logo.
}
