// Defines the properties (props) that the PageLayout component can accept.
export interface PageLayoutProps {
  children: React.ReactNode; // The content to be rendered within the layout.
  className?: string; // Optional: Custom CSS class for additional styling of the layout container.
}
